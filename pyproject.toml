[project]
name = "analytical-methods-vocabulary"
version = "0.0.1"
description = "Controlled vocabulary of analytical methods used for chemical analysis/profiling"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pubmate",
    "linkml",
]

[tool.uv.sources]
pubmate = { git = "https://github.com/eu-parc/pubmate.git", tag = "v0.0.2" }
